# xv6_demo

## Documentation

### Start Ubuntu

To start the Ubuntu Docker container interactively, use the following command:

```bash
docker start -i ubuntu
```

### Running Tests

To run the tests, ensure the following file is present in the directory:

```
.gdbinit.tmpl-riscv
```

This file is required for debugging and testing purposes.